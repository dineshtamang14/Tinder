apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinder.fullname" . }}
  labels:
    {{- include "tinder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinder.selectorLabels" . | nindent 4 }}

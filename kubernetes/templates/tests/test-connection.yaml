apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tinder.fullname" . }}-test-connection"
  labels:
    {{- include "tinder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tinder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
